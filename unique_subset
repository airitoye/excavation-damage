us <- function(v) {
  a <- length(unique(v))
  c <- c(1:a)
  for(i in 1:a){
    b <- unique(v)
    nam <- paste("subset_", b[i], sep = "")
    assign(nam, subset(df, v == b[i]))
  }
  t(data.frame(rbind(c, b)))
}
